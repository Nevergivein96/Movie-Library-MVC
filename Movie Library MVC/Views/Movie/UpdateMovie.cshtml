@model Movie

<h2>Update @Model.MovieName</h2>

@using (Html.BeginForm("UpdateMovieToDatabase", "Movie", "Post"))
{
        <input asp-for="MovieID" type="hidden" value="@Model.MovieID" />

        <label asp-for="MovieName" class="control-label">Name</label>
        <input type="text" name="MovieName" class="form-control" value="@Model.MovieName" />

        @*<div class="form-group">
            <label for="">Select a Category</label>
            <select class="form-control" name="GenreID" value="">
            @foreach (var cat in Model.GenreName)
            {
                        <option value="@cat.GenreID">@cat.GenreID @cat.GenreName</option>
            }
            </select>
        </div>*@

        @*<label asp-for="GenreName" class="control-label">GenreName</label>
        <input type="number" step="0.01" name="GenreName" class="form-control" value="@Model.GenreName" />

        <label asp-for="GenreName" class="control-label">GenreName</label>    
            <input type="number" step="0.01" name="GenreName" class="form-control" value="@Model.GenreName" />*@

        <label asp-for="RatingScale" class="control-label">RatingScale</label>
        <input type="number" step="0.01" name="RatingScale" class="form-control" value="@Model.RatingScale" />

        <input type="submit" value="Update Movie" class="btn btn-primary" />
}
